# Password Master 产品需求文档（PRD）

| 项 | 内容 |
| --- | --- |
| 产品名称 | Password Master |
| 文档版本 | v0.1 |
| 状态 | Draft |
| 日期 | 2026-08-31 |

---

## 1. 背景与目标

用户需要为账号、设备、服务生成足够随机、可配置的密码，避免弱密码与重复使用。Password Master 提供可控规则下的本地密码生成，强调**可预期、可复制、不联网存储密码**。

### 1.1 成功标准

- 用户能在 10 秒内生成符合所选规则的密码。
- 生成结果不可预测（密码学安全随机源），且规则与结果一致。
- 核心能力不依赖网络；不上传、不持久化明文密码（除非用户明确保存且采用安全存储，后续版本）。

---

## 2. 范围

### 2.1 In Scope（MVP）

- 按规则生成单条密码。
- 可配置：长度、字符集（大小写、数字、符号）、是否排除易混淆字符。
- 展示密码强度（弱 / 中 / 强）。
- 一键复制到剪贴板。
- 重新生成。

### 2.2 Out of Scope（MVP 不做）

- 云同步、账号体系、多人协作。
- 密码保险箱 / 加密存储（可作为 V1.1）。
- 从历史密码「破解」或「猜测」已有密码。
- 浏览器自动填充、系统钥匙串深度集成（可作为后续）。

---

## 3. 用户与场景

| 角色 | 诉求 |
| --- | --- |
| 个人用户 | 注册/改密时快速生成强密码并复制 |
| 开发者 | 为测试环境生成符合策略的密码 |

### 主要场景

1. 打开应用 → 调整长度与字符类型 → 生成 → 复制 → 粘贴到目标站点。
2. 站点要求「至少 12 位、含大小写数字符号」→ 勾选对应选项 → 生成直到强度达标。
3. 需要避免 `0/O`、`1/l/I` 混淆 → 开启「排除易混淆字符」→ 生成。

---

## 4. 功能需求

### 4.1 密码生成（P0）

- 输入：长度 `L`（默认 16，范围建议 8–64，可配置上下限）。
- 字符集开关（至少选一类，否则提示错误）：
  - 大写 `A–Z`
  - 小写 `a–z`
  - 数字 `0–9`
  - 符号（定义明确集合，例如 `!@#$%^&*()-_=+[]{};:,.?/`，并在 UI 展示）
- **保证**生成结果至少包含每种已启用类型中的 1 个字符（避免「勾了数字却全是字母」）。
- 随机源：使用操作系统/运行时提供的 CSPRNG（如 `crypto.getRandomValues` / `SecureRandom`），禁止 `Math.random()` 等弱随机。
- 操作：生成、再次生成。

### 4.2 排除易混淆字符（P0）

- 可选。开启后从字符集中移除易混字符，建议集合：`0 O o 1 l I |`。
- 若排除后某类字符为空，应提示并阻止生成，或自动降级并说明。

### 4.3 强度指示（P1）

- 基于长度与字符集多样性给出等级（弱/中/强），规则需写进实现注释或配置，避免黑盒。
- 仅作提示，不替代用户策略。

### 4.4 复制（P0）

- 一键复制当前密码。
- 复制成功有短暂反馈；失败时提示（权限/环境限制）。
- 可选（P2）：复制后 N 秒清空剪贴板（需说明平台限制）。

### 4.5 批量生成（P2，非 MVP）

- 一次生成 N 条（如 1–20），列表展示，支持逐条复制。

### 4.6 预设策略（P2）

- 例如：PIN（仅数字 6 位）、Wi-Fi、高安全（20 位全字符集）。

---

## 5. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 安全 | 明文密码仅存在于内存/当前会话 UI；不写日志、不上报分析 |
| 隐私 | 无账号则无服务端；分析若有，不得包含密码内容 |
| 性能 | 单次生成 < 50ms（常规长度） |
| 可用性 | 默认选项即可得到可用强密码；错误信息可操作 |
| 可访问性 | 对比度达标；复制/生成可用键盘操作 |
| 平台 | MVP 先定一种（Web / 桌面 / 移动），文档中锁定后再实现 |

---

## 6. 界面与交互（概要）

- 主界面：长度滑块/数字框、四类字符开关、排除易混淆、生成按钮、密码展示区（可显示/隐藏）、强度条、复制按钮。
- 空/错误态：未选字符集、长度非法、排除后字符集为空。
- 密码默认掩码显示，提供「显示」切换。

---

## 7. 验收标准（MVP）

- [ ] 默认参数下每次生成长度正确、字符集符合开关。
- [ ] 启用的每一类字符在结果中至少出现一次。
- [ ] 关闭某类后，结果中不含该类。
- [ ] 排除易混淆开启后，结果不含约定易混字符。
- [ ] 复制可将当前密码写入剪贴板。
- [ ] 连续生成结果不相同（统计意义上；极短空间除外需在文档注明）。
- [ ] 不使用弱伪随机数。

---

## 8. 风险与开放问题

- **平台**：Web、Electron/Tauri 还是原生？影响剪贴板与随机 API。
- **符号集**：是否允许用户自定义符号，避免站点禁止某些字符。
- **密码短语**：是否需要 diceware 类「多个单词」模式（V1.1）。
- **保存**：若做保险箱，需独立安全设计（主密码、加密、备份）。

---

## 9. 里程碑建议

| 阶段 | 内容 |
| --- | --- |
| MVP | 生成 + 规则 + 强度 + 复制 |
| V1.1 | 预设策略、批量、自定义符号、可选本地加密保存 |
| V1.2 | 密码短语、浏览器扩展 / 系统集成 |

---

## 10. 附录：建议默认值

- 长度：16
- 大写 / 小写 / 数字：开
- 符号：开
- 排除易混淆：关
